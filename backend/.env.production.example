# MoneyFlow Backend - Production Environment Example
# Bu dosyayı kopyalayıp .env.production olarak düzenleyin ve gizli tutun.

# Database (PostgreSQL)
DATABASE_HOST=your-postgres-host
DATABASE_PORT=5432
DATABASE_USER=moneyflow_prod
DATABASE_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DATABASE_NAME=moneyflow_production

# Redis
REDIS_HOST=your-redis-host
REDIS_PORT=6379

# JWT
JWT_SECRET=CHANGE_ME_LONG_RANDOM_STRING
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=CHANGE_ME_ANOTHER_LONG_RANDOM_STRING
JWT_REFRESH_EXPIRES_IN=7d

# Admin Credentials (PROD)
ADMIN_USERNAME=owner
# Üretimde düz şifre kullanmayın. ADMIN_PASSWORD yerine hash kullanın.
# ADMIN_PASSWORD=DO_NOT_USE_IN_PRODUCTION
ADMIN_PASSWORD_HASH=$2b$12$PUT_YOUR_BCRYPT_HASH_HERE

# App
PORT=3000
NODE_ENV=production

# Security
SECURITY_WEBHOOK_URL=

# CORS
# Üretim domain(leri)nizi girin. Virgül ile birden fazla ekleyebilirsiniz.
CORS_ORIGIN=https://app.your-domain.com
CORS_CREDENTIALS=true

# Frontend base URL used in emails (verification/reset/invite)
FRONTEND_URL=https://app.your-domain.com

# Require email verification to login (önerilir)
EMAIL_VERIFICATION_REQUIRED=true

# Not: Şu an EmailService simüle çalışır. PROD için bir sağlayıcı entegre etmeyi planlayın (SendGrid/SES/SMTP).
