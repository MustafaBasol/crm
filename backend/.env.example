# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=moneyflow
DATABASE_PASSWORD=your_database_password
DATABASE_NAME=moneyflow_dev

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT - GÜVENLİ ANAHTAR KULLANIN!
JWT_SECRET=your_super_secret_jwt_key_min_256_bits_long_very_secure_key_here
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your_refresh_token_secret_key_different_from_access_token
JWT_REFRESH_EXPIRES_IN=7d

# Admin Credentials - MUTLAKA HASH'LENMİŞ ŞİFRE KULLANIN!
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2b$10$hash_of_your_admin_password_here

# App
PORT=3000
NODE_ENV=development

# CORS (virgülle ayırın, sadece production'da kullanılır)
# Örnek: https://app.mycompany.com,https://admin.mycompany.com
CORS_ORIGINS=

# CSRF
# Production'da sabit ve güçlü bir değer sağlayın
CSRF_SECRET=

# ============================
# Email & Auth Flow Settings
# ============================
# E-posta sağlayıcısı: "ses" | "smtp" | "log"
MAIL_PROVIDER=log
# Gönderici adresi (SES'te doğrulanmış olmalı)
# Örn: Comptario <noreply@comptario.com>
MAIL_FROM=Comptario <noreply@comptario.com>
# Yanıt adresi (opsiyonel, SES'te doğrulanmış olması önerilir)
MAIL_REPLY_TO=support@comptario.com
# AWS Bölgesi (SES için)
AWS_REGION=eu-central-1
# (Opsiyonel) SES Configuration Set adı (CloudWatch metrics, event destination, vb. için)
SES_CONFIGURATION_SET=

# Frontend temel URL (doğrulama/sıfırlama linkleri burada oluşturulur)
FRONTEND_URL=http://localhost:5174

# E-posta doğrulaması zorunlu mu?
EMAIL_VERIFICATION_REQUIRED=true

# Token ömürleri ve yeniden gönderim bekleme süresi
VERIFICATION_TOKEN_TTL_HOURS=24
RESET_TOKEN_TTL_MINUTES=60
RESEND_COOLDOWN_SECONDS=60

# Parola politikası
PASSWORD_MIN_LENGTH=12
PASSWORD_MIN_SCORE=3

# Auth endpoint rate limitleri (dakika başına, IP başına)
SIGNUP_RATE_LIMIT=5
FORGOT_RATE_LIMIT=5
RESET_RATE_LIMIT=5
VERIFY_RATE_LIMIT=10
RESEND_RATE_LIMIT=5
WEBHOOK_SNS_RATE_LIMIT=120

# SNS Bounce/Complaint entegrasyonu (doküman: backend/SES_SNS_BOUNCE_COMPLAINT.md)
SNS_TOPIC_ARN_BOUNCE=
SNS_TOPIC_ARN_COMPLAINT=
# HTTPS subscription endpoint'i için paylaşılan gizli anahtar (opsiyonel)
SNS_WEBHOOK_SHARED_SECRET=

# Admin suppression UI/endpoint (listeleme/silme)
ADMIN_ALLOWED_IPS=127.0.0.1,::1,localhost

# ============================
# Security Headers / CSP
# ============================
# Opsiyonel CSP nonce üretimi (script-src 'nonce-...') etkin olsun mu?
<<<<<<< Updated upstream
SECURITY_ENABLE_CSP_NONCE=false
=======
SECURITY_ENABLE_CSP_NONCE=false

# ============================
# Stripe Ödeme Ayarları
# ============================
## ÖNEMLİ: Aşağıdaki örnek test anahtarları sadece geliştirme içindir. Üretimde gerçek secret key'i .env.production dosyasına koyun.
## Format HATASI düzeltildi: Değerleri ayrı satırda yazmayın; KEY=VALUE şeklinde olmalı.
STRIPE_SECRET_KEY="YOUR_STRIPE_TEST_SECRET"
STRIPE_PUBLISHABLE_KEY="sk_test_PLACEHOLDER"
STRIPE_WEBHOOK_SECRET="STRIPE_WEBHOOK_SECRET_PLACEHOLDER"
STRIPE_MODE=test
# Başarılı/iptal yönlendirmeleri için temel URL (frontend)
# FRONTEND_URL zaten mevcutsa onu kullanırız; opsiyonel sane defaults
PAYMENT_SUCCESS_PATH=/payments/success
PAYMENT_CANCEL_PATH=/payments/cancel
WEBHOOK_STRIPE_RATE_LIMIT=300

# Plan Fiyat ID'leri (Stripe Dashboard > Prices)
# Starter
STRIPE_PRICE_STARTER_MONTHLY=
STRIPE_PRICE_STARTER_YEARLY=
# Pro
STRIPE_PRICE_PRO_MONTHLY=price_1SRGBEK8pwobn5mA40QBZIBq
STRIPE_PRICE_PRO_YEARLY=price_1SRGBbK8pwobn5mAYBkauzD5
# Business (isteğe bağlı, genelde sales-contact)
STRIPE_PRICE_BUSINESS_MONTHLY=
STRIPE_PRICE_BUSINESS_YEARLY=

# Add-on: Ek kullanıcı (koltuk) fiyat ID'leri
STRIPE_PRICE_ADDON_USER_MONTHLY=price_1SRGF0K8pwobn5mALeu8wsgD
STRIPE_PRICE_ADDON_USER_YEARLY=price_1SRGFpK8pwobn5mAlnF18S2a
>>>>>>> Stashed changes
